# Idreaml Clip（理梦剪藏）

Idreaml Clip 是一款面向桌面端的本地优先剪切板管理工具。它会自动保存你复制过的文本，让临时复制的链接、代码、文案和重要信息不再因为下一次复制而丢失。

无论是整理资料、编写文档、处理代码，还是在多个应用之间频繁搬运内容，Idreaml Clip 都能帮助你快速找回、搜索并再次使用历史剪切板内容。

## 产品介绍

### 自动记录，随时找回

Idreaml Clip 在后台自动记录文本剪切板内容。复制过的信息会按照最近使用时间排列，无需提前分类或手动保存。

相同内容会自动合并，并记录使用次数，避免历史列表出现大量重复条目。

### 快捷面板，提高复制效率

通过全局快捷键 `Ctrl + Shift + V` 可以随时呼出快捷面板，不必离开当前正在使用的应用。

快捷面板支持键盘操作：

- 使用 `↑`、`↓` 选择内容；
- 按 `Enter` 将选中内容重新写入剪切板；
- 按 `Esc` 关闭快捷面板；
- 输入关键词快速查找历史内容。

### 搜索、收藏与历史管理

全部历史支持关键词搜索和今日内容筛选。经常使用或不希望被自动清理的内容，可以加入收藏。

不再需要的记录可以删除，也可以在隐私页面一次性清空历史。删除操作会保留必要的同步标记，确保其他设备不会重新恢复已经删除的内容。

### 本地优先，数据由你掌控

Idreaml Clip 不要求注册账号，也不依赖云端服务。即使没有网络或未配置同步，剪切板记录、搜索、收藏和快捷面板仍然可以完整使用。

所有剪切板内容首先保存在本机数据库中。你可以随时暂停记录，并自行决定是否启用云同步。

### 使用自己的仓库同步

需要在多台设备间共享剪切板时，可以连接自己的 Gitee、GitHub 或 GitLab 私有仓库。

Idreaml Clip 内置 Git 能力，不要求电脑预先安装 Git。同步数据采用可审阅的 JSON / JSONL 文件保存，并支持：

- 手动立即同步；
- 应用启动后自动同步；
- 每 5、10 或 30 分钟定时同步；
- 多设备内容去重；
- 收藏、删除和最近使用状态合并；
- 查看参与同步的设备和最后活跃时间。

访问令牌只保存在本机的系统安全凭证中，不会写入剪切板数据库或同步仓库。同步异常也不会影响本地剪切板的正常使用。

### 安静地运行在桌面后台

关闭主窗口后，Idreaml Clip 会继续在系统托盘中运行并监听剪切板。你可以通过托盘菜单重新打开主窗口，也可以根据需要开启开机自动启动。

## 使用介绍

### 1. 开始记录

启动 Idreaml Clip 后，应用会自动监听新的文本剪切板内容。像平时一样在任意应用中复制文本即可，无需额外操作。

如果暂时不希望保存复制内容，可以点击左下角的“暂停记录”，或前往“隐私”页面关闭记录。

### 2. 查看和搜索历史

打开“全部历史”页面即可查看保存过的剪切板内容。

- 在顶部搜索框输入关键词查找内容；
- 使用“今天”筛选当天复制的记录；
- 点击一条记录查看完整内容和使用信息；
- 点击复制按钮，将内容重新放入剪切板。

### 3. 收藏常用内容

在历史列表或内容详情中点击星标，即可将内容加入“我的收藏”。

收藏适合保存邮箱、常用回复、代码片段、地址等需要反复使用的信息。再次点击星标即可取消收藏。

### 4. 使用快捷面板

在任意应用中按下 `Ctrl + Shift + V` 呼出快捷面板。

你可以直接输入关键词搜索，也可以使用方向键选择记录并按 `Enter` 使用。快捷面板支持只显示收藏内容，适合快速访问高频文本。

### 5. 配置云同步

启用云同步前，建议先在 Gitee、GitHub 或 GitLab 创建一个专门用于 Idreaml Clip 的私有空仓库，并准备具有该仓库读写权限的访问令牌。

配置步骤：

1. 打开“云同步”页面；
2. 选择 Gitee、GitHub 或 GitLab；
3. 填写仓库的 HTTPS 地址；
4. 填写 Git 用户名和访问令牌；
5. 确认同步分支，默认为 `main`；
6. 保存连接后点击“立即同步”；
7. 根据需要选择 5、10 或 30 分钟的自动同步间隔。

请不要把访问令牌拼接在仓库地址中。令牌需要单独填写在访问令牌输入框内。

### 6. 管理同步设备

完成同步后，可以在“设备”页面查看已加入同一仓库的设备、操作系统和最后同步时间。

新设备只需安装 Idreaml Clip，并连接同一个仓库和分支，即可导入已有记录。

### 7. 调整存储与桌面设置

在“设置”页面可以调整：

- 快捷面板显示的记录数量；
- 本地历史记录保存上限；
- 历史内容自动清理周期；
- 开机自动启动；
- 关闭窗口后的后台运行行为。

收藏内容不会被常规的自动清理规则移除。

### 8. 退出应用

点击窗口关闭按钮时，应用默认隐藏到系统托盘并继续工作。

如需完全退出，请在系统托盘中找到 Idreaml Clip 图标，并选择“退出”。

## 使用建议

- 为云同步使用独立的私有仓库，不要与其他项目文件混用；
- 为访问令牌仅授予同步仓库所需的最小权限；
- 涉及密码、验证码等敏感内容时，可以临时暂停剪切板记录；
- 第一次在新设备上连接仓库后，先执行一次“立即同步”；
- 分发或移动 Windows 版本时，请保留 Release 目录中的全部文件，不要只复制可执行文件。
