# Idreaml Clip（理梦剪藏）

Idreaml Clip 是一款面向桌面端的本地优先剪切板管理工具。它会自动保存你复制过的文本，Windows 版也支持截图和图片，让临时复制的内容不再因为下一次复制而丢失。

无论是整理资料、编写文档、处理代码，还是在多个应用之间频繁搬运内容，Idreaml Clip 都能帮助你快速找回、搜索并再次使用历史剪切板内容。

安装步骤见 [离线图文使用指南](doc/Idreaml-Clip-1.0.3-使用指南.html)。Windows 版支持 Windows 10/11 x64；Mac 测试包目标为 Apple Silicon（M 系列）与 macOS 12+，使用免费 ad-hoc 本地签名，不包含苹果公证。当前 Mac 功能范围是本地文本记录、搜索、收藏、JSON 预览和快捷面板；图片采集与写回、自动粘贴、同步令牌存储尚未适配。构建和下载方式见 [桌面打包说明](doc/packaging.md)。

## 产品介绍

### 自动记录，随时找回

Idreaml Clip 在后台监测系统剪切板变化，不依赖 Ctrl+C 或 Ctrl+V 按键。Windows 版可记录纯文本、截图、浏览器“复制图片”和复制的 PNG/JPG/GIF/BMP/WebP 图片文件。复制过的信息会按照最近使用时间排列，无需提前分类或手动保存。

图片在历史列表使用较大的矩形预览，按原比例完整显示；快捷面板显示缩略图，详情支持缩放查看，复制时写回系统图片格式。详情右上角随所选内容显示 TEXT、JSON、PNG、JPG 等类型；JSON 对象或数组需要解析有效，原始文本保持不变。系统位图截图转换为 PNG，有原始图片编码时保留其格式。单张编码图片上限 32 MB，解码上限 4000 万像素，超限时显示提示。

相同内容会自动合并，并记录使用次数，避免历史列表出现大量重复条目。

### 快捷面板，提高复制效率

通过全局快捷键可以随时呼出快捷面板。Windows 默认 `Ctrl + Shift + V`，macOS 默认 `Control + Option + V`，Linux 默认 `Ctrl + Alt + V`。在“设置 → 全局快捷键”中点击录入区域，直接按下想使用的完整组合（例如 `Alt + Q`、`Ctrl + Alt + J` 或单个 `F8`），松开全部按键后保存。修饰键和主键均可更换，支持平台可注册的字母、数字、标点、方向键及功能键。保存后立即生效，重启后保留，也可以恢复默认。录入期间旧快捷键暂停生效，取消编辑后恢复；`Esc` 可取消本次录入。Windows 会检查组合是否被占用或保留。面板采用约 347 × 427 的紧凑尺寸，在鼠标附近显示，并自动避开屏幕边缘和任务栏。

Windows 版另提供“设置 → 固定 Win + V 快捷键”开关，默认关闭。开启后，在应用运行期间拦截 `Win + V` 并呼出本应用的快捷面板，自定义快捷键置灰且停止生效，原组合仍独立保留。关闭开关后恢复原自定义组合；开关状态在重启后保留。该选项不出现在 macOS/Linux 中。拦截随应用退出释放，不修改 Windows 的剪切板历史设置。锁屏、UAC 安全桌面不属于此功能范围。

单击条目即可复制并收起面板；Windows 下通过快捷键唤起前若有可编辑的输入光标，会返回原输入位置自动粘贴。没有输入目标或目标应用不接受粘贴时，内容仍保留在系统剪切板。右击条目可选择“复制”（仅复制）或“删除”。点击面板外部或按 `Esc` 可关闭面板。

快捷面板采用无边框窗口，内容紧贴窗口边缘。鼠标悬停在图片或有效 JSON 条目上时，会在旁边弹出预览，并根据屏幕剩余空间选择左右位置。图片按原始横竖比例直接展示，不带标题、类型标签或外框，可缩放查看；JSON 自动缩进格式化并支持滚动。复制和粘贴仍使用原始内容。移出条目及预览区域后，预览自动收起。

快捷面板支持键盘操作：

- 使用 `↑`、`↓` 选择内容；
- 按 `Enter` 使用选中内容，与单击条目相同；
- 按 `Esc` 关闭快捷面板；
- 输入关键词快速查找历史内容。

### 搜索、收藏与历史管理

全部历史支持关键词搜索和今日内容筛选。经常使用或不希望被自动清理的内容，可以加入收藏。

选中 JSON 记录时，内容详情左右分栏：左侧保留原始文本，右侧以语法配色、行号和层级引导线展示 JSON。点击对象或数组前的箭头可单独折叠和展开，也可使用“展开全部”“折叠全部”。两侧独立滚动，深层内容支持横向滚动。右侧“复制格式化 JSON”始终复制完整解析结果，底部“复制内容”仍复制原文。

JSON 识别支持标准转义引号、整体转义但省略外层引号的文本，以及多次编码的 JSON 字符串。解析仅还原整段文本的外层编码，保留字段内字符串、反斜杠路径和转义字符的含义；原始剪切板记录保持不变。快捷面板的 JSON 预览使用同一识别规则。

拖动历史列表与内容详情之间的分隔线，可调整列表和预览宽度。左侧导航栏边界也可以拖动，缩窄后保留图标入口，悬停可查看名称；向右拖动恢复文字。布局会保存在本机，切换页面或重启后保留。

“设置 → 历史保存数量”提供 1,000、5,000、10,000、50,000 条预设，也可选择“自定义”输入任意正整数。保存后立即生效并在重启后保留，超出的最旧非收藏记录会被自动清理。

不再需要的记录可以删除，也可以在隐私页面一次性清空历史。删除操作会保留必要的同步标记，确保其他设备不会重新恢复已经删除的内容。

### 本地优先，数据由你掌控

Idreaml Clip 不要求注册账号，也不依赖云端服务。即使没有网络或未配置同步，剪切板记录、搜索、收藏和快捷面板仍然可以完整使用。

所有剪切板内容首先保存在本机数据库中。你可以随时暂停记录，并自行决定是否启用云同步。

### 使用自己的仓库同步

需要在多台设备间共享剪切板时，可以连接自己的 Gitee、GitHub 或 GitLab 私有仓库。

Idreaml Clip 内置 Git 能力，不要求电脑预先安装 Git。同步数据采用可审阅的 JSON / JSONL 文件保存，并支持：

- 手动立即同步；
- 应用启动后自动同步；
- 每 5、10 或 30 分钟定时同步；
- 多设备内容去重；
- 收藏、删除和最近使用状态合并；
- 查看参与同步的设备和最后活跃时间。

访问令牌只保存在本机的系统安全凭证中，不会写入剪切板数据库或同步仓库。同步异常也不会影响本地剪切板的正常使用。

### 安静地运行在桌面后台

关闭主窗口后，Idreaml Clip 会继续在系统托盘中运行并监听剪切板。左键单击托盘图标进入“全部历史”；右键菜单依次提供“退出程序”“打开快捷剪切板”“进入全部历史”。点击桌面或菜单外部即可关闭菜单，无需选择选项。悬停标题为 `Idreaml Clip`。你也可以根据需要开启开机自动启动。

## 使用介绍

### 1. 开始记录

启动 Idreaml Clip 后，应用会自动监听新的剪切板内容。像平时一样在任意应用中复制文本、复制图片或截图到剪切板即可，无需额外操作（图片采集目前支持 Windows）。

如果暂时不希望保存复制内容，可以点击左下角的“暂停记录”，或前往“隐私”页面关闭记录。

### 2. 查看和搜索历史

打开“全部历史”页面即可查看保存过的剪切板内容。

- 在顶部搜索框输入关键词查找内容；
- 使用“今天”筛选当天复制的记录；
- 点击一条记录查看完整内容和使用信息；
- 点击复制按钮，将内容重新放入剪切板。

### 3. 收藏常用内容

在历史列表或内容详情中点击星标，即可将内容加入“我的收藏”。

收藏适合保存邮箱、常用回复、代码片段、地址等需要反复使用的信息。再次点击星标即可取消收藏。

### 4. 使用快捷面板

在任意应用中按下配置的快捷键（Windows 默认 `Ctrl + Shift + V`），或使用托盘右键菜单的“打开快捷剪切板”呼出面板。管理页面不提供快捷面板入口。

你可以单击记录，也可以使用方向键选择记录并按 `Enter`；Windows 下有原输入光标时会自动粘贴，否则只复制。悬停图片和 JSON 记录可查看预览。右击记录可以仅复制或删除；点击窗口外部或按 `Esc` 收起面板。输入关键词可搜索全部历史，面板也支持只显示收藏内容。

### 5. 配置云同步

启用云同步前，建议先在 Gitee、GitHub 或 GitLab 创建一个专门用于 Idreaml Clip 的私有空仓库，并准备具有该仓库读写权限的访问令牌。

配置步骤：

1. 打开“云同步”页面；
2. 选择 Gitee、GitHub 或 GitLab；
3. 填写仓库的 HTTPS 地址；
4. 填写 Git 用户名和访问令牌；
5. 确认同步分支，默认为 `main`；
6. 保存连接后点击“立即同步”；
7. 根据需要选择 5、10 或 30 分钟的自动同步间隔。

请不要把访问令牌拼接在仓库地址中。令牌需要单独填写在访问令牌输入框内。

### 6. 管理同步设备

完成同步后，可以在“设备”页面查看已加入同一仓库的设备、操作系统和最后同步时间。

新设备只需安装 Idreaml Clip，并连接同一个仓库和分支，即可导入已有记录。

### 7. 调整存储与桌面设置

在“设置”页面可以调整：

- 快捷面板显示的记录数量；
- 本地历史记录保存上限；
- 历史内容自动清理周期；
- 开机自动启动。

收藏内容不会被常规的自动清理规则移除。

### 8. 退出应用

点击窗口关闭按钮时，应用默认隐藏到系统托盘并继续工作。

如需完全退出，请右击系统托盘中的 Idreaml Clip 图标，并选择“退出程序”。

## 使用建议

- 为云同步使用独立的私有仓库，不要与其他项目文件混用；
- 为访问令牌仅授予同步仓库所需的最小权限；
- 涉及密码、验证码等敏感内容时，可以临时暂停剪切板记录；
- 第一次在新设备上连接仓库后，先执行一次“立即同步”；
- 分发或移动 Windows 版本时，请保留 Release 目录中的全部文件，不要只复制可执行文件。
